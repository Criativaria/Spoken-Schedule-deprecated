import { View } from "lucide-react-native";

export function SearchBar({ typed, where }) {
  //typed: o que o usuário digitou, where: aonde ele quer filtrar, (canais ou programas)
  return (
    //retorna: array com coisas filtradas
    <View></View>
  );
}
